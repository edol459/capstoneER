to run extract_keypoints_yolo.py
you will need to install YOLO


pip install torch torchvision numpy matplotlib polars pyyaml pillow psutil requests scipy ultralytics-thop
pip install opencv-python-headless


on line 12, you can input the video folder that you need to extract

then running 3d_vis.ipynb will check if the model was able to extract the poses.
